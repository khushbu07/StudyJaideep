package Java.JavaExceptions;

/*
 * ClassNotFoundException occurs when you try to load a class at runtime using Class.forName() or loadClass() methods 
 * and requested classes are not found in classpath.
 */
public class checked_ClassNotFoundException {

}
