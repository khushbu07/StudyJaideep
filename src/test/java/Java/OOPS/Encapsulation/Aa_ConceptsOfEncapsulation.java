package Java.OOPS.Encapsulation;

/*
 * Explanation:
 * Encapsulation in Java is a mechanism to wrap up variables(data) and methods(code) together as a single unit
 * to avoid external modifications.
 * By making use of "private" access modifier and Getter-Setter methods to access the private variables.
 * 
 * Main Purpose is to restrain user to manipulate the variables
 * By keeping the variables as Private & methods public so that user can only access the methods
 */

public class Aa_ConceptsOfEncapsulation {

}
