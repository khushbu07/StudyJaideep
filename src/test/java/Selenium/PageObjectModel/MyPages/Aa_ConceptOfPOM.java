package Selenium.PageObjectModel.MyPages;

/*
 * Explanation:
 * In Page Object Model, we store page class of or web application in a separate package.
 * And in every page class, we define Objects (WebElements) locators and their action methods.
 * And we call these Locators & their methods in different test classes defined in a separate package.
 * 
 */
public class Aa_ConceptOfPOM {

}
